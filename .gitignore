# Dependencies
node_modules/

# Build output
dist/
build/
*.local

# Environment (keep examples tracked)
.env*
!.env.example
!.env.production.example

# Logs & debug
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Vite
.vite/

# Test / coverage
coverage/
.nyc_output/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor / IDE
.idea/
*.swp
*.swo
*~

# Optional: uncomment if you add TypeScript
# *.tsbuildinfo

# Database WAL/SHM temp files — always ignore
backend/data/careculator.db-shm
backend/data/careculator.db-wal
backend/src/data/careculator.db-shm
backend/src/data/careculator.db-wal

# The main .db file is tracked via Git LFS on the deploy branch.
# On other branches it remains untracked (too large for regular git).
# backend/data/careculator.db  ← intentionally NOT ignored here on deploy branch
